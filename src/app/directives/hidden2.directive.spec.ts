import { Hidden2Directive } from './hidden2.directive';

describe('Hidden2Directive', () => {
  it('should create an instance', () => {
    const directive = new Hidden2Directive();
    expect(directive).toBeTruthy();
  });
});
